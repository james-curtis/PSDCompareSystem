<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.compare.mapper.CompareMapper">


    <resultMap id="compareAccount" type="com.example.compare.entity.Compare">
        <id column="id" property="id"></id>
        <result column="serial_number" property="serialNumber"></result>
        <result column="order_id" property="orderId"></result>
        <result column="create_time" property="createTime"></result>
        <result column="status" property="status"></result>
        <result column="work_code" property="workCode"></result>

    </resultMap>

    <select id="search" resultMap="compareAccount">
        select *from order_log where serial_number ='${param1}'
                                 and (create_time BETWEEN '${param2}' AND '${param3}') limit ${param4},${param5};
    </select>

    <delete id="allDelete">
        delete order_log,compare from compare
          left join order_log on compare.order_id=order_log.id
          where compare.order_id='${param1}';
    </delete>

    <select id="getOrderIdByid" resultType="java.lang.String">
        select o.out_trade_id from compare c
        left join order_log o
        on c.order_id = o.id
        where c.id = #{id}
    </select>
</mapper>